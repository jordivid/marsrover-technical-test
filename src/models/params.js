export class Params {
  constructor(params) {
    this.gridHeight = params[0];
    this.gridWidth = params[1];
    this.xPos = params[2];
    this.yPos = params[3];
    this.orientation = params[4];
    this.commands = params[5];
  }
}
